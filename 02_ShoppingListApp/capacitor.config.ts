import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02_ShoppingListApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
